#!/usr/bin/env bash

set -e

language=$(echo $@ | cut -d '/' -f 1)
exercise=$(echo $@ | cut -d '/' -f 2)
docker compose run \
    -it --rm \
    --workdir "/home/exercist/exercism/$language/$exercise" \
    "${language:-default}" sh
